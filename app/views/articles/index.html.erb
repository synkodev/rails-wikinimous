<h1>All articles</h1>
<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %> - <%= link_to 'Delete', article_path(article), data: {turbo_method: :delete, turbo_confirm: 'Do you want to delete this article?'} %></li>
  <% end %>
</ul>

<%= link_to "Add new article", new_article_path %>
